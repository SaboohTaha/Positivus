---
const steps = [
  {
    number: "01",
    title: "Discovery",
    description: "We dive deep into your business goals, user needs, and market landscape to build a solid strategy."
  },
  {
    number: "02",
    title: "Design",
    description: "Our creative team crafts intuitive and stunning interfaces that align with your brand identity."
  },
  {
    number: "03",
    title: "Development",
    description: "We build robust, scalable solutions using modern technologies and best coding practices."
  },
  {
    number: "04",
    title: "Launch & Scale",
    description: "We ensure a smooth launch and provide ongoing support to help your product grow."
  }
];
---

<section id="process" class="py-24 bg-slate-50 relative">
  <div class="layout-padding">
    <div class="text-center max-w-3xl mx-auto mb-20">
      <h2 class="text-3xl md:text-5xl font-bold text-slate-900 mb-6">Our Process</h2>
      <p class="text-lg text-slate-600">A proven methodology to deliver exceptional results, every time.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 relative z-10">
      {steps.map((step, index) => (
        <div class="relative p-8 bg-white rounded-2xl shadow-sm hover:shadow-lg transition-shadow duration-300 border border-slate-100 group">
          <div class="text-6xl font-black text-slate-100 absolute top-4 right-4 z-0 group-hover:text-indigo-50 transition-colors duration-300 select-none">
            {step.number}
          </div>
          <div class="relative z-10">
            <h3 class="text-xl font-bold text-slate-900 mb-4 mt-8">{step.title}</h3>
            <p class="text-slate-600 leading-relaxed">{step.description}</p>
          </div>
          <!-- Connector line for desktop (visual only, simple CSS trick) -->
          {index < steps.length - 1 && (
            <div class="hidden lg:block absolute top-1/2 -right-6 w-12 h-0.5 bg-slate-200 -z-10 translate-x-2"></div>
          )}
        </div>
      ))}
    </div>
  </div>
</section>
